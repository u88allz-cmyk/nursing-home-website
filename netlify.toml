[build]
  command = "cp -r dist client/ && echo 'Static files ready'"
  publish = "client/dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "20"